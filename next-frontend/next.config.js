// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false, // Optional: Disable 'X-Powered-By' header for security
};

module.exports = nextConfig;


